/*项目 开关 gradle配置*/
ext {
    switches = [
            // 现在 Gradle
            // 正式环境 和 测试环境
            isRelease: false,
            // 是否生成version.xml文件 打版的时候改为ture
            execute  : true
    ]
    // 正式环境 和 测试环境 服务器 URL 配置
    url = [
            "debug"  : "https://192.188.22.99/debug",
            "release": "https://192.188.22.99/release"
    ]
}